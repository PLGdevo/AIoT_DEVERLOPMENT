# Middleware

Middleware connects HybridAI to external systems.

Supported/planned integrations:

- MQTT
- HTTP
- Serial
- Modbus
- ROS 2

ROS 2 is intentionally an integration boundary rather than a dependency
of the core HybridAI library.
